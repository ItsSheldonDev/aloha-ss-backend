# .env.test - Configuration pour l'environnement de test
NODE_ENV=test
PORT=3001

# Base de données de test (utiliser une base de données séparée pour les tests)
DATABASE_URL="postgresql://user:pass@localhost:5432/aloha_secourisme?schema=public"

# JWT
JWT_SECRET=test_jwt_secret_for_testing_purposes_only
JWT_EXPIRES_IN=1h

# SMTP (factice pour les tests)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=test@example.com
SMTP_PASS=test_password

# Admin pour les tests
ADMIN_EMAIL=test-superadmin@example.com
ADMIN_PASSWORD=password123